SOLANA_RPC_URL="https://api.devnet.solana.com"
PORT=4000
# Paste keypair JSON directly (no file path). Either use this OR AUTHORITY_KEYPAIR_PATH.
# AUTHORITY_KEYPAIR_JSON="[1,2,3,...]"
# Or use a file path:
AUTHORITY_KEYPAIR_PATH="/home/adarsh/.config/solana/id.json"
ALLOWED_ORIGINS="http://localhost:3000"
